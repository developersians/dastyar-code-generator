using Ddd.Kernel.Domain;
using MediatR;

namespace Arzfy.Application;

public sealed record Create{{ PascalCase }}Command(
    {{~ for prop in Properties ~}}
    {{~ if prop.OriginalName != "ID" ~}}
    {{ prop?.DataType }} {{ prop?.PascalCase }}{{ if !for.last }}, 
{{ end }}{{ end }}{{~ end ~}}) : IRequest<Result<short>>;
